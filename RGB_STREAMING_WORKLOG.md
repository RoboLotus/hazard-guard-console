# RGB 영상 전송 고도화 작업 기록

- 이슈: https://github.com/RoboLotus/hazard-guard-console/issues/29
- 브랜치: `perf/rgbd-streaming-pipeline`
- 목적: 최신성·영상 연속성을 우선으로 유지하며 화질/FPS/자원 비용을 비교한다.
- 기록 원칙: 날짜별로 추가한다. 계획, 구현, 로컬 검증, 실물 실측을 구분하고 미측정 값을 성과로 쓰지 않는다.
- 범위: Console의 RGB 미리보기. Robot, SLAM, Depth 측정값, 열화상 판정, 주행·배출 정책은 변경하지 않는다.

## 2026-09-10 — 기존 JPEG 예비 기준선 (기존 산출물 참조)

- 정지 벽 장면, 640×480, Overview 0/1/2개, 조건별 약 60초 1회.
- FastAPI CPU 평균 44.36/48.91/52.05% (1코어=100%). 선형 증가를 입증한 결과가 아니다.
- JPEG 생성 약 20회/s, 화면 하나의 RGB HTTP 응답 약 3.34회/s. 결과 공유는 이미 존재한다.
- 기준선 코드 `2543d281c3347de9e024057b94a01b64987f789d`; 현재 작업 시작점 `11c8b59`와 다르다. 직접 개선율 계산 금지, 동일 코드 기준 재측정 필요.
- 원시 산출물: 워크스페이스 `outputs/jpeg-baseline-20260910/`. 대용량 결과와 개인정보는 이 저장소에 반입하지 않는다.
- 촬영→표시 최신성, 차트 품질, H.264/VP8 종단 성능은 미측정.

## 2026-09-11 — 연구 근거 및 실험 설계 개정 (기존 산출물 참조)

- AoI 서베이: 갱신이 멈춰도 화면 정보 나이를 계속 측정한다.
- Performance of AV1 Real-Time Mode: 입력 간격과 구현을 통제하고 처리량·지연을 분리한다.
- SQP: 정상→제한→회복, 느린 수신자의 영향을 확인한다. SQP 자체 구현 계획은 아니다.
- 기존/주기제어 JPEG는 필수 후보. H.264·VP8은 브라우저 경로 실현성 확인 후 본시험 후보 선택.
- 접속 0/1/2/4/8/12, RGB 전용과 전체 Overview 분리. 전체 영상 처리 프로세스 비용을 비교.
- 계획/기록표: https://docs.google.com/spreadsheets/d/1uL-YhD-rbNNayHQUepv1leEfVFSGMMrsnJgIC2iGd3U/edit

## 2026-09-11 — 상시 경량 계측 구현 착수

### 승인된 설계

1. 영상 응답에 세션·프레임 번호·시간 메타데이터를 함께 전달한다. 메타데이터 전용 폴링은 만들지 않는다.
2. 브라우저에서 영상 정지 중에도 나이를 계측하고 5초 단위로 통계를 전송한다.
3. 기본은 경량 집계. 상세 진단만 제한된 원시 표본을 포함한다. 재시도·큐·클라이언트·파일 용량에 상한을 둔다.
4. 로그 실패가 영상 표시를 막지 않게 한다. 원본 이미지·토큰·자유 입력 메시지는 저장하지 않는다.

### 구현 시 주의 / 범위 조정

- ROS stamp의 시계/촬영 의미는 실물에서 미검증. 기본은 백엔드 수신→브라우저 표시 근사이며 촬영 AoI로 표기하지 않는다.
- 브라우저와 젯슨의 벽시계를 직접 빼지 않는다. 응답 시점의 프레임 나이와 브라우저 단조 시계로 투영하고 요청 경과시간으로 전송 불확실성 범위를 제공한다.
- RGB는 응답 헤더를 읽기 위해 `img src` 직접 폴링을 fetch/blob 경로로 바꿔야 한다. 이 자체의 비용과 느린 응답에서 요청이 겹치지 않는 정책도 새 기준선의 일부다.
- 이번에는 JPEG 품질/인코딩 주기 변경, 코덱 교체, Jetson 배포·부하 시험을 하지 않는다.

### 완료·검증 결과

### 구현 내역

- `backend/app/stream_observability.py`: RGB 수신 시각 및 응답 헤더, 엄격한 집계 스키마, 8KiB 입력 상한, 클라이언트별 최소 4초 수신 간격, 단조 보고 순서 검사.
- `stores.py` / `ros_media.py`: 이미지와 함께 저장되는 세션·번호·수신 시각. 요청할 때 번호를 새로 만들지 않는다. ROS stamp를 보존하되 기본은 미검증이다.
- `frontend/src/rgbTelemetry.js`: 100ms 관측, 평균/p95/최대 및 히스토그램, 오래됨·미수신·숨김·관측 불가 시간 분리. 같은 이미지 반복 수신을 새 프레임으로 세지 않는다.
- `frontend/src/rgbPreview.js` / `Common.jsx`: RGB만 fetch/blob 경로, 요청 동시 1개, 5초 타임아웃, 화면 제거 시 AbortController/타이머/object URL 정리. 지도·열화상 표시 경로는 기존 유지.
- 정상 보고 5초 주기. 전송 타임아웃 2초, 실패 시 최대 60초 backoff, 미전송 데이터를 무한 큐잉/재전송하지 않음. 구형 서버의 404/405는 해당 화면의 보고를 중단하되 영상은 유지.
- 백엔드 기본 JSONL 순환 저장(원본 영상·자유 문자열·토큰 없음). 최대 64개 최근 클라이언트, 128개 쓰기 큐. 15초 이상 보고가 없으면 `report_missing`; 5분 넘은 정보는 조회에서 제외하고 다음 수신 때 정리.
- 창 단위 `fresh/stale/missing/unobserved/request_error` 및 `state_changed` 기록. 순간 안전 이벤트나 모터 제어에 사용하지 않는다.
- 추가 Python/npm 의존성 없음. Node 설치는 잠금 파일의 기존 의존성만 사용.

### 로컬 검증 기록

- 백엔드 전체 `python -m pytest -q`: 254개 통과 (로컬 Windows 가상환경; 실물 ROS 미기동).
- 프런트 전체 `node --test tests/*.test.mjs`: 102개 통과를 3회 확인. 첫 전체 실행에서 1개 실패가 있었지만 출력 축약으로 실패 이름을 보존하지 못했고, 이후 3회는 재현되지 않음. 새 단위 테스트는 별도 실행에서도 통과. 이후 추가 검증 결과는 아래에 추가한다.
- `npm run build`: 통과. 기존 큰 번들(500kB 초과) 경고는 남음.
- 실제 내장 브라우저 + 로컬 fixture에서 RGB 이미지 표시/집계 수신 확인. 같은 JPEG 반복 시 `frames_loaded>0`, `unique_frames=0`, 나이·stale 시간이 증가하는 것을 확인.
- 브라우저 실험에서 5초 타이머에 중복 시간 가드를 두면 미세한 지터로 10초마다 보고되는 문제 발견. 정상 주기는 타이머 하나가 담당하도록 수정 후 약 5초 창으로 복귀 확인.
- 확인은 기능 smoke test이며 Jetson CPU 절감·촬영 지연·화질 성과가 아니다.

### 최종 확인 추가 (2026-09-11)

- 미수신 전환 시 영상 숨김, `missing_ms` 증가, `age.p95_ms=null` 확인. 정상 복구 후 새 프레임 증가와 `missing_ms=0`/`stale_ms=0`으로 복귀했다. 화면 제거 시 이미지 요소가 제거되는 것도 확인했다.
- 실서비스 페이지의 기존 RGB 요청 간격 300ms와 공용 컴포넌트 기본 간격 500ms를 유지했다. JPEG 인코딩 품질과 입력 콜백 주기는 변경하지 않았다.
- 기존 공간 상태 API 테스트가 앞선 단계 전환 테스트의 공유 상태에 의존해 간헐적으로 실패했다. 해당 테스트에만 독립 `SpatialStore`를 주입해 격리했다. 실물 지도·위치 추정 코드는 수정하지 않았다.
- 최종 전체 회귀 검사: 백엔드 254개, 프런트 104개 통과. 프런트 빌드도 통과(기존 500kB 초과 번들 경고 유지).
- 기존 프런트 패키징 테스트는 `dist` 산출물이 필요하므로 깨끗한 worktree에서는 `npm run build` 이후 전체 테스트를 실행한다. 첫 실패의 원본 출력은 보존되지 않아 이것이 최초 실패 원인이었다고 단정하지 않는다.
- 로컬 fixture와 전용 Vite 서버는 검증 후 종료한다. Jetson 배포, 성능 측정, commit/push는 이번 실행 범위에 포함하지 않았다.
- 이후 작업도 이 파일에 날짜 / 가설·변경 이유 / 변경 파일·설정 / 명령·결과 / 미검증 사항을 추가한다. 수치 성과는 실제 측정 파일과 실행 조건을 확보한 경우에만 기재한다.

## 상시 운영 설정 / 로그 해석

| 환경변수 | 기본값 | 의미 |
|---|---|---|
| `HAZARD_GUARD_STREAM_DIAGNOSTICS` | `basic` | 백엔드 `off/basic/detailed`; 재시작 시 적용 |
| `VITE_HAZARD_GUARD_STREAM_DIAGNOSTICS` | on | `off`면 브라우저 샘플링·보고 중단. 프런트 재빌드/개발 서버 재시작 필요 |
| `HAZARD_GUARD_STREAM_LOG_PERSIST` | `1` | `0`이면 메모리 집계만, 디스크 기록 없음 |
| `HAZARD_GUARD_STREAM_LOG_DIR` | `backend/runtime/stream-observability` | 운영자가 지정하는 로그 디렉터리. 비밀값이나 로그는 git에 올리지 않음 |
| `HAZARD_GUARD_RGB_STAMP_CLOCK` | 미설정 | 실제 ROS stamp가 젯슨 Unix 시각과 일치함을 검증한 경우만 `unix_verified` |

- 완전한 계측 ON/OFF 비용 비교는 백엔드와 프런트 모두 같은 설정으로 켜거나 끈다. fetch/blob 표시 경로는 동일하게 남아 계측 자체의 추가 비용을 분리한다. 기존 img 직접 폴링과의 비교는 별도 시험이다.
- 기본 `reference=ingress`: ROS 콜백 수신부터의 시간. `cache`: 수신 메타데이터 없는 이미지의 캐시 생성 이후. `capture`: 검증됐다고 명시된 ROS stamp로 계산. `mixed`: 한 창에서 기준이 달라졌으므로 촬영 AoI 단일값으로 비교하지 말 것.
- 브라우저의 마지막 표시 프레임 나이 상한 근사 = 서버 응답 시 나이 + 요청 시작~본문 수신 시간 + 본문 수신 이후 브라우저 단조 시간. 요청 시간은 통신 불확실성 상한으로 별도 보고한다. 네트워크 편도 지연을 정확히 측정한 값이 아니다.
- `age.mean_ms/p95_ms/max_ms`는 균일 간격 관측값의 통계. 숨김 탭, 500ms 넘는 타이머 공백, 메타데이터 없는 영상은 관측 불가로 구분한다. 이 구간을 최신성 성공으로 계산하지 않는다.
- 노후/미수신 비율은 `(stale_ms + missing_ms) / observed_ms`. 반드시 `observed_ms/window_ms`도 함께 보고한다. 관측 커버리지가 부족하면 합격 판정을 하지 않는다. 전 구간 미수신은 p95=0이 아니라 null.
- `age` 통계는 보수적 상한 기반이고, 촬영→표시의 실제값을 보증하지 않는다. 동일 기준·네트워크에서 비교하고 시계/전송 불확실성도 병기한다.
- 히스토그램은 표본 수로 합산 가능하지만 창별 p95를 평균내어 전체 p95로 쓰지 않는다. 버킷 기반 통합 p95는 근사치로 표시한다. 상세 모드는 최근 50개 나이 값만 기록하므로 전체 원시 프레임 trace가 아니다.
- 클라이언트 ID는 화면 컴포넌트 수명마다 익명으로 생성한다. 인증 수단이 아니며 화면을 닫은 경우도 `report_missing`이 된다. 보고 누락은 카메라 고장 확정이 아니다. 브라우저 자기 보고를 안전 제어 판단에 사용하지 않는다.
- 로그 저장은 전용 daemon 쓰레드. 큐 포화/디스크 오류는 `dropped_logs`/`write_errors`로 노출하고 영상 처리와 분리한다. 파일은 2MiB, 백업 3개(총 약 8MiB). 종료 시 미처리 큐가 남을 수 있어 완전한 감사 로그가 아니다.

### 하드웨어 없는 로컬 기능 검증 재현

1. backend 디렉터리의 기존 Python 환경에서 `python -m scripts.rgb_observability_smoke --port 8891` 실행.
2. frontend에서 `HAZARD_GUARD_BACKEND_URL=http://127.0.0.1:8891` 설정 후 `node node_modules/vite/bin/vite.js --host 127.0.0.1 --port 8767 --strictPort` 실행.
3. `http://127.0.0.1:8767/tests/rgb-preview-harness.html` 접속. 정상/동일 프레임 정지/미수신/제거·복원을 확인한다.
4. fixture는 ROS/모터/디스펜서를 import·실행하지 않으며 loopback에만 바인딩한다. 파일 영구 기록도 끈다. 기능 확인 후 두 서버를 종료한다.

### 당시(2026-09-11) 아직 수행하지 않은 일

- Jetson 배포, ROS stamp의 시계 및 실제 촬영 의미 검증.
- 계측 ON/OFF 자원 및 전송량 비교. 위 구현이 싸다고 아직 수치로 주장하지 않는다.
- 기존 직접 이미지 폴링과 새 fetch/blob 경로의 비용 비교 및 동일 버전 새 기준선.
- 4/8/12개 접속, 느린 수신자, 제한 대역폭, 차트 품질 시험.
- JPEG 인코딩 주기 제어와 H.264/VP8 브라우저 경로 구현·선별.
- 장애 직전 모든 프레임의 상세 추적/장기간 분석 시스템은 아직 없음. 필요 시 별도 범위로 추가.

## 2026-09-11 — VNC에서 확인할 RGB-D 화면 준비

- 사용자 요청으로 SSH 접속 후 Jetson의 카메라 전용 드라이버와 RViz를 실행했다. 이번 계측 코드의 배포나 백엔드 실행은 하지 않았다.
- 재부팅 후 HP60C USB는 인식됐지만 카메라 노드는 없었다. 설치된 제조사 `ascamera hp60c.launch.py`를 사용했다(설정 파일 수정 없음).
- `ROS_DOMAIN_ID=162`, `ROS_LOCALHOST_ONLY=1`. 기존 VNC 데스크톱 `DISPLAY=:0`에 RViz RGB/Depth Image 디스플레이를 배치했다. 주행·Nav2·YOLO·디스펜서는 실행하지 않았다.
- 드라이버 설정은 RGB/Depth 640×480, 25FPS. `ros2 topic hz`로 RGB 약 23FPS 수신을 확인하고 Depth 헤더 1건 수신도 확인했다. 이는 짧은 화면 준비 점검이며 성능 기준선이나 VNC 표시 FPS가 아니다.
- 토픽: `/ascamera_hp60c/camera_publisher/rgb0/image`, `/ascamera_hp60c/camera_publisher/depth0/image_raw`. RViz는 Best Effort 구독.
- 임시 설정 `/tmp/hazard-rgbd-camera-20260911.rviz`, 로그 `/tmp/hazard-rgbd-view-camera.log`, `/tmp/hazard-rgbd-rviz.log`. 사용자 VNC 확인용으로 켜둔다. 향후 기준선 측정에서는 RViz/VNC 실행 상태를 시험 조건으로 통일해야 한다.
- 같은 날 16:55 재부팅 후 사용자 요청으로 카메라/RViz를 다시 실행했다. USB와 VNC 데스크톱이 정상임을 먼저 확인했고, 부팅으로 사라진 `/tmp` RViz 설정을 복원했다. 자동 부팅 서비스나 시스템 설정은 변경하지 않았다.

## 2026-09-14 — RGB-D 현장 화면 재실행

- 19:25경 사용자 요청으로 USB 인식과 VNC 세션을 확인한 뒤 카메라 전용 launch와 RViz를 다시 실행했다. 기존 카메라/RViz 프로세스는 없었다.
- 이전과 동일한 도메인 162·localhost 한정 설정으로 실행. `/tmp/hazard-rgbd-camera-20260914.rviz`를 사용했다.
- RGB/Depth 각각 최신 메시지 헤더 수신과 데스크톱 RViz 창 생성을 확인했다. 사용자 확인용으로 켜두었으며, 주행·백엔드·YOLO·디스펜서 실행이나 성능 실험은 하지 않았다.
- 이후 사용자 `No image` 신고로 재점검: 커널 로그에서 19:25:34 USB 장치 8 disconnect → 장치 10 재인식을 확인했다. 카메라 프로세스는 살아 있으나 USB 제어 전송 실패와 `uvc_start_streaming: No such device`를 반복했고 RGB 메시지 수신은 시간 초과했다. RViz만 재실행하는 것으로는 해결되지 않았다.
- 카메라 launch를 종료하고 프로세스 소멸 확인 후 같은 설정으로 재실행했다. 종료 과정에는 제조사 libusb mutex assertion(exit -6)도 기록됐다. 기존 오류 로그는 보존하고 복구 로그는 `/tmp/hazard-rgbd-camera-reconnect-20260914.log`로 분리했다. USB 재연결의 물리적 원인(전원/케이블/사용자 재연결)은 이 로그만으로 확정하지 않는다.
- 복구 후 약 12초 RGB 주기 검사에서 약 16~19FPS를 관측하고 Depth 헤더도 다시 수신했다. 단기 메시지 복구 확인이며 장기 안정성 또는 VNC의 실제 표시 FPS를 뜻하지 않는다.

## 2026-09-14 — 인쇄 차트 설치 후 실물 1차 기준선

### 실행 범위와 이유

- 사용자 인쇄물 설치 완료 후 시험 요청. 차트 PNG를 `outputs/rgb-chart-20260914/reference.png`에 확보했다. 상단 일부 잘림·상단 반사광이 있어 이번 회차는 자원/최신성/전송 경향 평가로 한정하고 정밀 화질 점수는 주장하지 않는다.
- 시험 직전 19:37:29 USB 재연결 후 프레임 미수신이 다시 확인돼 카메라를 재기동했다. RViz는 종료해 그 부하를 제외했다. VNC 서버와 MAXN_SUPER 전력 모드는 유지했다. 기존 domain 61 제조사 조이스틱 노드는 수정하지 않았고, 시험 domain 162는 RGB 구독만 한다.
- `backend/scripts/rgb_pipeline_benchmark.py`: 운영 main/ROS bridge를 import하지 않는 별도 FastAPI. 실제 `RosMediaAdapter.on_rgb_image`/JPEG Q82 코드를 사용하지만 합성 열화상 처리는 시험 경로에서 명시적으로 제외했다. 따라서 전체 운영 백엔드 또는 과거 Overview 수치와 직접 개선율을 계산하지 않는다.
- `frontend/tests/rgb-live-benchmark.html`: 실제 `startRgbPreview`를 사용해 300ms 주기 영상 뷰를 0/1/2/4/8/12개 생성한다. **하나의 브라우저·하나의 페이지·공유 연결 풀** 시험이며 독립 사용자/로봇 12대를 재현한 것은 아니다. 12뷰도 모두 화면에 보이는 작은 미리보기로 배치한다.
- 소스 기준 `11c8b5976cde5115bf2c57744812fb2dab9adc59` + 미커밋 계측 변경. 전송한 소스 tar SHA256 `16DCBD0E8E259C371B43AB18087246270B99019475A688A31777521D6FABDE4F`. 운영 checkout 수정/의존성 설치 없음.
- 원격 임시 경로 `/tmp/hg-rgb-20260914.eovlMU`; 기존 Python 가상환경 재사용. 제어 API는 시험 단계 시작과 상태 조회만 있으며 로봇 동작 API는 없다. 전송은 현재 원격 연결 + 로컬 Vite 프록시. 네트워크 조건 강제 제한/변경 없음.

### 1차 프로토콜

- 차트 고정·640×480·드라이버 설정 25FPS 유지. 목표 요청 간격 300ms(약 3.33회/s), 원래 인코딩 주기 유지.
- 순서: 무접속 → 1뷰 OFF/ON/OFF/ON → ON 2/4/8/12뷰 → 1뷰 회복.
- 각 단계 워밍업 15초 + 관측 60초. ON/OFF는 각 2회, 확장 조건은 각 1회인 **예비 측정**. 3분×3회 본시험으로 표현하지 않는다.
- 시스템/코어/프로세스 CPU·메모리 1초 간격, tegrastats GPU/온도/전력 기록. 샘플러는 동일 FastAPI 프로세스에 있으므로 그 비용도 백엔드 CPU에 포함한다.
- 두 모드 모두 단계 처리시간 probe는 켜두고, 상시 계측용 헤더/브라우저 집계/보고/로그 비용만 ON/OFF한다. 서버 로그 스레드의 유휴 비용은 OFF에도 남을 수 있다.
- 원시 phase JSON에 처리 횟수·시간 통계·브라우저 5초 창·자원 표본을 저장. CPU 전체=6코어 평균(전체100%), 프로세스=1코어100% 기준.
- 카메라 프레임 나이 >5초, 가용 RAM<500MiB 또는 CPU 온도≥85℃면 단계 중단. USB/센서 중단 회차는 성능 비교에 사용하지 않는다.
- `summarize_rgb_benchmark.py`는 히스토그램 표본을 합쳐 p95 **버킷 범위**를 반환하며 창별 p95를 평균내지 않는다. 보고 도착 시간/tegrastats에 기반한 시작 경계는 근사이므로 경계 창을 제외한다. OFF에는 브라우저 최신성 통계가 없으며 0ms로 기입하지 않는다.
- 전송 오버헤드는 추가 메타데이터 헤더+보고 JSON body 기준이다. HTTP 기본 헤더/TCP/VPN 등을 포함한 링크 전체 바이트가 아니므로 완전한 네트워크 사용량으로 쓰지 않는다.

### 결과

- 10단계 완료(약 19:46–19:59 KST). 각 60개 자원 표본, 중단 없음. 결과 판정은 **제한사항을 붙인 예비 기준선**이며 최종 최신성 합격은 보류한다.
- 백엔드 CPU 평균(1코어100%): 0뷰 25.78%, 1뷰 ON 28.35%/27.41%, 2뷰 28.58%, 4뷰 31.57%, 8뷰 34.53%, 12뷰 39.58%, 1뷰 회복 27.33%. Jetson 전체(6코어 평균)는 1뷰 약14.4%→12뷰21.57%.
- 실제 입력과 JPEG 인코딩 약20FPS. 0뷰도 인코딩 약20FPS를 유지했다. 표시 갱신은 뷰당 약3.16–3.24FPS. 측정 콜백 내부 JPEG 평균5.4–5.7ms, 전체 콜백6.3–6.7ms. **다음 후보는 표시 수요/인코딩 주기 분리**이며 아직 최적화/절감 효과를 구현·검증한 것은 아니다.
- JPEG payload 전송: 1뷰 ON 약139–141KiB/s, 12뷰1,665KiB/s. Tailscale CPU도 1뷰3.27%→12뷰24.12%(1코어100%)로 증가했다. 전체 CPU 증가를 FastAPI만의 문제로 해석하지 않는다.
- 상시 계측 추가 바이트는 JPEG 대비 약0.49%, 1뷰 약700B/s. ON/OFF 각2회 평균CPU 차이는 +0.38%p(1코어100%)로 반복 변동보다 작다. 유의한 추가 비용/무비용 어느 쪽도 확정하지 않는다.
- 표시 나이 근사 p95 버킷은300–500ms였으나 최대1.45초, stale(>1초)/missing 비율은 채택 창에서 최대1.27%. HTTP 왕복/본문수신 불확실성도 최대1.12초로 원인을 인코딩 하나로 확정하지 않았다.
- 채택 보고 창 커버리지79.1–91.6%, 채택 창 내부 관측률99.66–100%. 제외 구간을 성공으로 세지 않는다. 정확한 촬영 종단 지연도 아니다.
- 백엔드RSS 평균157.7→162.9→회복161.6MiB, CPU 온도 최대57℃, 평균전력약5.4–5.6W, Swap0MiB. 짧은 시험으로 메모리 누수/장기 안정성을 보증하지 않는다.
- 최종 진단 dropped_logs=0, write_errors=0, queue_size=0. 카메라 로그에는 측정 전19:42 MJPEG 조회 오류가 있어 보존했고, 수집 USB 이벤트 파일은 비어 있었다. 측정 중 어댑터 오류/HTTP503/센서 stale 가드 중단은 없었다.
- 원시 결과+실제 배포 소스 압축 약168KiB, 기준PNG1장만 보관. ROS bag/연속 원본 영상은 녹화하지 않았다.

### 재현 파일·QA·남은 점

- 로컬 산출물: `D:/Develop/hazard-guard/outputs/rgb-chart-20260914/README.md`, `raw/results/summary.csv`, `results.tar.gz`, `reference.png`, `validation.ipynb`. 결과 README에 전체조건·전송량·최신성·한계·다음 실험을 기재했다. 파일은 git에 추가하지 않았다.
- 검증: 원시10단계/60표본,640×480,CPU분모,클라이언트 수,보고ID중복,히스토그램합 및 CPU평균/p95 재계산 assertion 통과. Jupyter 커널은 Windows ZeroMQ `Bad file descriptor`로 실행 실패해 순차 Python 셀 실행으로 대조했다. notebook에 이 제한을 명시했다.
- 종료 후 ROS context 이중 shutdown 오류가 기록됐다. 저장된 측정값은 그대로 보존하고 시험 스크립트 종료를 `try_shutdown`으로 보완했다. 다음 회차용 정확한 begin/end monotonic 경계도 추가했다. **두 보완은 측정 이후 변경이며 Jetson에서 재실행 검증하지 않았다.**
- 요약기 explicit boundary 우선 테스트 추가. 백엔드 전체 **259개 통과**, 프런트 RGB telemetry **11개 통과**. 처음 일반 Anaconda로 전체 테스트를 실행했을 때 FastAPI 미설치로 수집 실패했으며, 기존 WebUI backend 가상환경으로 재실행해 통과했다. 의존성 추가 설치는 하지 않았다.
- 시험 백엔드8001/로컬 시험Vite8767 종료, RViz 재실행 후 RGB/Depth 헤더 수신 확인. Depth 단건 echo에 메시지 유실 알림1회도 있어 깊이 안정성 검증으로 표현하지 않는다. 주행명령/운영 checkout 수정/commit/push 없음.
- 다음: phase 경계·마지막 창 회수와 요청/본문수신/decode 구간 계측 보강 → 같은 해상도/Q82/목표FPS에서 수요 기반 최신프레임 인코딩 비교 → 독립 수신자/느린 수신자/제한대역폭 시험. 차트 상단 잘림·반사광 조정 후 정밀 품질 비교. H.264/VP8 후보 시험은 아직 진행하지 않았다.

## 2026-09-14 — 목표 10FPS 재측정 및 방식별 비교 프레임 보관

### 요청과 실행 원칙

- 사용자 요청: RViz 종료, 방식별 웹 수신 이미지 1장 보관/시트 삽입, 약10FPS 목표로 재측정. **10FPS는 실험 목표이며 보편적 최적값/달성 보장이 아니다.** 운영 Overview 기본값은 아직 바꾸지 않는다.
- 같은 JPEG Q82·640×480, 기존 입력마다 인코딩 구조 유지. 시험 하네스만 `?profile=10fps`로 전환. 현재 차트 배치가 앞선 회차와 달라 같은 회차에3.33FPS 기준선을 다시 측정한다.
- 1뷰3.33FPS → 10FPS 1/2/4/8/12뷰 → 10FPS1뷰 회복, 각15초준비+60초관측. 한 페이지 공유 연결 풀 조건이며 독립 사용자/로봇 수로 해석하지 않는다. 마지막 보고 창 회수/구간별 전송 타이밍은 이번에도 완전하지 않아 커버리지를 별도로 기록한다.
- 기존 VNC 유지, RViz 종료. 시험 전20:19:59 USB11 disconnect→20:20:05 장치15 재인식 후 제조사 드라이버가 No such device 반복. 기존 카메라 launch만 정상 종료를 기다린 후 재실행, 실제RGB 수신 확인 후시험시작. 주행/임무/YOLO/디스펜서는 실행하지 않았다.
- 임시 경로 `/tmp/hg-rgb-10fps-20260914`, 소스 tar SHA256 `B8C3B4638420D7DC0AD86B93C541F6A0739F850664D231FF32FACA88ECE05C45`.
- 시험용 ROS context가 Uvicorn과 SIGINT를 중복 처리하지 않도록 ROS signal handler를 끄고 Uvicorn 종료 경로에서 try_shutdown. 실제 운영 ROS 초기화 코드와 별개다.

### 비교 이미지 보관 규칙

- `GET /api/bench/snapshot?label=...`은 현재 warming 단계에만 허용. 기존 웹 JPEG 응답의 바이트를 그대로 파일에 저장하고 동일 바이트를 브라우저에 반환한다. 스크린샷 축소/재인코딩 없음.
- JPEG+JSON 쌍에 실행ID,codec/quality,해상도,targetFPS,뷰수,저장시간,프레임헤더,SHA256을 저장. 각 단계 준비 중1장만 수집하여 측정 구간 전송량에 섞지 않는다. 기존 파일 덮어쓰기 거부.
- 원본은 로컬보관하고 Google Sheet `비교 프레임`에 조건과 함께 표시. 동일JPEG에서FPS만 바꾸면 개별 이미지 품질은 원칙상 동일 조건이며 센서노출/조명차이를 코덱 효과로 부르지 않는다.
- 이후 방식 변경도1장/조건 보관. H.264 등 영상 코덱은 브라우저 디코드 프레임을 무손실PNG로 보관하고 원 코덱/비트레이트/키프레임/시점 등을 기록할 계획. 아직 해당경로는 미구현. 한 장으로 모션품질/프레임유실을 판정하지 않는다.

### 결과

- 실행ID `1789384953858`, 7단계 완료, 자원 표본420개. 같은 회차 기준1뷰3.33FPS 목표에서 실제3.18FPS / 백엔드CPU27.44% / 전체CPU14.36%였다.
- 목표10FPS 실제 표시FPS(뷰당): 1뷰8.25, 2뷰8.19, 4뷰7.54, 8뷰6.67, 12뷰4.73, 1뷰회복8.24. 백엔드CPU 평균은30.33/33.36/38.05/45.88/46.38/30.44%(1코어100%)였다.
- 최신성 p95 구간: 기준3.33FPS 300–500ms → 10FPS1/2뷰750–1000ms → 4/8/12뷰1000–1500ms. 1뷰회복도750–1000ms. 평균 나이 개선과 p95 악화를 구분한다. **10FPS 목표는 유지하되 운영 기본값 즉시 변경은 보류**한다.
- 최대 표시 나이 근사5.12초(8뷰), stale/missing 최대11.51%(12뷰). 채택 창에서8뷰 영상요청오류1/보고오류4,12뷰 보고오류2가 있었다. HTTP503/어댑터오류/센서가드 중단은0회였다. 전송/수신 실패와 센서 입력 중단은 다르다.
- 채택 보고 창 커버리지83.20–91.54%. 채택 창 내부 관측률은약100%지만 제외 창은 성공으로 세지 않는다. 로그쓰기오류/큐드롭0, 종료직전 큐0. 최신성 최종 합격 판정은 하지 않는다.
- JPEG 전송량1뷰 약356KiB/s,8뷰2369KiB/s,12뷰2487KiB/s. 전체CPU는12뷰에서도26.49% 평균으로 포화하지 않았다. 요청/본문수신 불확실성도최대약4초였으므로 CPU 단독 병목으로 확정하지 않고 브라우저 연결 풀/원격망/프록시/서버 대기를 다음에 분리한다.
- 이미지7장+JSON, 원시로그+배포소스 압축약451KiB를 `D:/Develop/hazard-guard/outputs/rgb-10fps-20260914/`에 보존했다. `README.md`, `raw/results/summary.csv`, `validation.ipynb`, `validate_run.py`, `drive-manifest.json` 참조. SHA256·640×480·Q82·워밍업 촬영 시점·CPU 평균/p95·보고ID/히스토그램 검사 통과.
- Google Sheet에 `0914 FPS 비교`(sheetId187349937), `비교 프레임`(sheetId616454528) 추가. 기존 탭 보존. JPEG7장은 Drive에 영구 원본 업로드 후 해당행에 링크 연결. IMAGE() 임시 URL은 외부URL 허용 문제로#REF였고, 브라우저도로그아웃 상태였다. 만료URL을 제거하고7개 HYPERLINK로 대체해 정상표시값과수치읽기검증. **셀안에썸네일직접삽입/Google 렌더 시각 검증은 미완료**이며 원본보기/다운로드 링크 방식이다. 파일 공개권한 변경 없음.
- 백엔드259개, 프런트RGB11개 테스트 및 문법검사 통과. 데이터 검증은Python순차실행이며 notebook은출력보존용; Jupyter커널재실행은미검증이다.
- 시험서버/Vite 종료, RViz 종료 유지. ROS context 중복종료 보완 후 실물로그에서 정상 application shutdown 확인. 카메라 드라이버만 남겼다. 운영checkout/기본FPS/주행정책 변경 및 commit/push 없음.

### 2026-09-14 — 실험 자료 폴더 및 파일명 정리

- 사람이 읽는 분류본: `D:/Develop/hazard-guard/outputs/RGB_영상전송_실험자료/2026-09-14/`. `00_먼저읽기.md`에서 시작하며 결과보고서 / 비교이미지 / 원시로그 / 재현코드로 구분했다.
- JPEG 7장은 `01_기준선_목표3.33FPS_화면1개.jpg` 등의 시험 순서·목표 FPS·화면 수 이름으로 복사했다. JSON도 같은 이름으로 묶고 원본과 SHA256 일치를 확인했다. 목표 FPS와 실측 FPS, 화면 수와 독립 사용자 수는 다르다고 안내했다.
- 기존 두 회차 폴더는 분석 코드 및 링크 재현성을 위해 보존했다. 분류본은 보고서 링크, CSV, JPEG/JSON, 원시 결과 압축, 측정 당시 소스 압축을 제공한다. 기존 결과를 수정하거나 새로 측정하지 않았다.
- Drive는 `RGB 영상 전송 성능 실험 / 2026-09-14_JPEG_3FPS와10FPS_비교 / 02_비교 이미지_640x480_Q82`로 7개 이미지를 이동·이름 변경했다. 기존 부모를 확인하고 이동 후 7개 파일의 부모와 이름을 재조회했다. 파일 ID 및 기존 시트 링크 유지, 공개 권한 변경 없음.
- Drive 이미지 폴더: https://drive.google.com/drive/folders/1mb40pI5XW6PimD-1SRyckMSfM6PHNueG
- 이후 날짜·회차·방식별로 쌓고, 조건별 이미지와 메타데이터를 짝으로 저장하는 규칙을 안내 파일에 기록했다.

## 2026-09-14 — JPEG 주기제어 A/B 및 구간별 지연 예비 실험

- 사용자 계속 실험 요청. `perf/rgbd-streaming-pipeline` worktree에서 시험 전용 코드만 추가했다. 운영 RosMediaAdapter·미리보기 기본 정책은 이번 변경에서 수정하지 않았다. 기존 미커밋 변경 보존, commit/push 없음.
- `rgb_encode_gate.py`: 입력 수신을 유지하면서 표시용 변환·JPEG 인코딩을 주기 제한. 대기열 없음, 시계 지연 후 과거 작업 몰아서 실행하지 않음. 수요 기반 워커는 아직 아님.
- `rgb_pipeline_benchmark.py`: 시험 단계의 ingress/periodic 옵션, 표시 인코딩 생략 횟수, 단계 종료 후 구간 타이밍 업로드(행수/유한값/파일 덮어쓰기 제한).
- 시험 전용 `rgb-benchmark-timing.js`로 기존 미리보기 플랫폼을 감싸 요청→헤더, 헤더→blob, blob→onload/rAF를 분리했다. 최대25,000행, 단계 종료 후 업로드. 운영 프런트 상시 로그 형식은 변경하지 않았다.
- 프로토콜: 같은640×480/Q82/목표10FPS, 1뷰ABBA→4뷰AB→12뷰AB. 준비15초+자원60초, 구간 타이밍55초 성공 프레임. 한 페이지 공유 연결 풀. 실행ID1789387713905, 약21:08~21:18KST.
- Jetson domain162/localhost-only, VNC/MAXN_SUPER 유지, RViz 없음, 주행/임무/YOLO/디스펜서 미기동. RGB-only 별도FastAPI. 열화상/Depth/운영백엔드 전체 시험이 아님.
- 준비 과정에서 PYTHONPATH 덮어쓰기로 rclpy import 실패1회. ROS setup 재적용 후 경로를 추가하도록 복구, 측정 전 오류로그 별도보존. 이후 RGB 수신 확인하고 시작.
- 1뷰 A백엔드CPU 평균30.34%→B23.42%(상대22.8% 감소),4뷰38.25→32.36%,12뷰45.53→40.00%. 1코어100% 기준. 전체6코어CPU는1뷰17.31→17.54%로 감소하지 않았음. 전체CPU가22.8% 줄었다고 표현하지 않는다.
- 인코딩약20→10FPS. 실제 새 화면FPS 1뷰A7.98/8.64,B7.78/6.65;4뷰A7.58,B6.68;12뷰A4.09,B4.29. B중복프레임 비율1뷰9~12%,4뷰13.48%,12뷰5.28%로 증가. 고정10Hz 인코딩/폴링 결합 개선 필요성을 관측했지만 원인 전체를 위상차로 확정하지 않는다.
- 12뷰 성공프레임 구간p95: 헤더대기A1068ms/B531ms, 본문약36ms, decode/rAF약6.5ms. 헤더 이전 병목 후보를 좁혔으나 연결풀/망/프록시/서버 책임은 미분리. 물리 촬영종단지연도 아님.
- 표시나이p95는1뷰 회차별300~1500ms 변동.12뷰A1500~2000ms,B1000~1500ms. 최신성 합격 및 운영반영 보류. CPU 절감만을 성공으로 판정하지 않음.
- 커버리지66.6~91.5%, 타이밍55초·자원60초·기존보고창 서로분모다름. A12 보고오류6회,B12 요청오류1회. 누락구간을 정상으로 취급하지 않는다. 초반에 로컬 단위테스트 실행이 있었으므로 완전히 통제된 수신PC 본시험이 아님. 후속 전용유휴PC/3회반복/느린수신자/재접속 검증 필요.
- 8단계480자원표본,8JPEG SHA256/640×480/Q82/촬영시점, 유한타이밍·숨김없음·행상한 검사통과. 센서가드/HTTP503/어댑터오류0, 수집USB이벤트없음. 최대CPU온도57.31℃. 백엔드261테스트, 프런트11+2테스트통과.
- 결과 `outputs/RGB_영상전송_실험자료/2026-09-14/03_JPEG_주기제어_AB비교/`: 실험설계.md,01_결과보고서/결과요약.md,CSV,상세통계,02_비교이미지,03_원시로그,04_재현코드,analyze.py. 압축617KiB,ROSbag없음. 원격/로컬 압축SHA256 3D38600C22E43AE7ED169821C77B096D0D01D5DA402D5433079C77836A879689 일치.
- 소스tar SHA256 8094840C57802E421921343EDC19E6A1DF010FD491227CC9F0EF8879A82FE393. 프런트시험소스 별도보존. 원시파일 수정없이 재계산 가능.
- GoogleSheet 기존탭보존, `0914 JPEG 주기제어`(2026091403) 추가.180개셀수치/링크/헤더서식 읽기검증. 고정열과노트병합 충돌로 첫batch실패(원자적미반영),고정열제거 후성공. 브라우저로그아웃으로 Google 렌더 시각확인은 미완료,행높이/열너비/줄바꿈은API로설정검사.
- Drive회차폴더 https://drive.google.com/drive/folders/1YeCBk13eiJ2Hf7zORBY8Yaha7FX4umLx . JPEG8개 및보고서/CSV업로드,이미지부모/파일이름재조회검증. 공개권한변경없음.
- 종료: 시험PID358963 정상 application shutdown,포트8001해제,로컬Vite31436종료. 기존카메라드라이버만유지,SSH종료.
- 다음은 동일JPEG에서 새프레임전달/대기열제한 및 요청대기구간 분리. 그 후 H.264 등 코덱 비교. 이번은 코덱 비교결과가 아님.

## 2026-09-14 야간 — 새 프레임 전달 구현, 조명 문제로 성능 측정 보류

- 시험 코드에만 async 새 프레임 대기(session/sequence), 최대1초/최신값1개/12대기자 wakeup, Server-Timing handler/wait/active, 클라이언트별 cursor를 추가했다. 운영 RGB 변환/표시 경로는 이번 변경에서 수정하지 않았다.
- 백엔드262개/프런트14개 테스트 통과. HTTP 실물검증12연결×3프레임, 미래sequence timeout503, 잘못된쿼리422, 구세션/재접속/poll200 확인. 실제센서분리/프로세스재부팅시험을 했다는 뜻은 아니다.
- 21:42 USB 재연결 후 기존 드라이버 No such device 반복을 확인하고 카메라 launch만정상종료/재기동하여 RGB 복구. 제조사 코드/ROS주행기능 변경없음.
- 23:40 영상이검고 JPEG5,930바이트여서 성능시험중단. 사용자도조명문제를확인하고다음날재개요청. 별도4차폴더에구현계획/기능JSON/검은원본JPEG보관. 원격로그압축은생성됐지만다운로드는사용자중단요청으로취소했다. Drive4차폴더만생성,시트수치갱신없음.
- 시험서버779887/로컬Vite14068/SSH종료. 카메라782816/782875유지. git commit/push없음.

## 2026-09-15 — 인쇄 차트 확인 후 전달 방식 비교 재개

- 10:00KST접속,인쇄차트확인. 다소어두운장면으로정밀화질판정은유보하고동일회차전달비교실시. 사전JPEG19,580바이트보존. 카메라재시작없이수신정상.
- 전날동일배포소스SHA256 `580F818D1A4001236DA6D827C94524E4F8AF2CBFA75481F3BB04AA370C0FEF45`,새경로`/tmp/hg-rgb-delivery-20260915`,PID2512757,domain162,localhost-only,RGB전용. 운영서버/주행/YOLO/디스펜서/RViz미실행.
- 실행ID1789434129857. 인코딩10FPS동일,1뷰ABBA→4뷰AB→12뷰AB,15초준비+60초측정. A=100ms폴링,B=새프레임대기후다음요청즉시. 같은async핸들러라과거sync회차대비개선율을계산하지않는다.
- 브라우저측정중추가단위테스트/빌드/분석배치미실행. 원시파일과조건별JPEG를날짜별새폴더에보관한다.
- 8단계완료,480자원표본/8타이밍/8JPEG SHA검증.1뷰평균FPS7.97→9.79,4뷰7.83→9.50,12뷰7.85→8.95. A중복10.6~16.2%→B0%. 백엔드CPU평균1뷰23.22→23.65,4뷰30.91→31.72,12뷰52.84→51.22%(1코어100%). CPU절감이주효과라고해석하지않는다.
- 12뷰나이상한p95 A200~300→B500~750ms,상한1초초과/미수신1.13→2.28%악화. 최대상한은3.11→1.45초감소. 최우선최신성문제로일괄운영반영보류. 기존공식에요청전체시간(프레임생성이전대기포함)이들어가므로물리종단지연이아니며B에보수적편향가능.
- 서버active p95약0.25~0.30ms(콜백인코딩제외).12뷰요청별헤더−handler p95 A66.5→B146.2ms. 연결풀/프록시/망/엔드포인트전대기후보는남아있고원인확정못함. 다음은12뷰반복/독립연결풀대조와나이계측보강.
- 영상요청/timeout/센서/어댑터오류0,B1두번째보고전송오류1. 커버리지83.20~91.56%,최대CPU온도56.59℃,RSS평균158→166MiB(장기메모리안정성미검증).USB검색결과없음.원본JPEG19,538~19,666바이트로정밀화질평가아님.
- 결과`outputs/RGB_영상전송_실험자료/2026-09-15/01_새프레임전달_AB비교/`,analyze.py/결과요약/CSV/상세통계/이미지/원시로그/재현코드.원시압축548KiB,SHA256 `00E04579CE5BFD586AC97EC408ECB0B33C137B8C9CE5F41CED888439FC31005D` 원격/로컬일치.
- 기존GoogleSheet에 `0915 새프레임 전달`(2026091501)새탭,225개셀읽기대조일치.기존결과보존.브라우저Google로그아웃으로네이티브시각검증은불가하여API서식검증.이미지8장/설계/결과md/CSV업로드: https://drive.google.com/drive/folders/1lW4fxRq3NlcALFnFbNFG7BzjZACrTFpP . 이미지Drive검색색인은빈결과여서개별ID메타데이터로검증.
- 시험서버2512757정상종료/8001해제,로컬Vite17652및시험탭종료.카메라드라이버유지,운영설정변경/commit/push없음.

## 2026-09-15 — 12뷰 연결 경로 분리 ABBA 반복

- 실행ID1789435678004, 10:28~10:33KST. RGB640×480/Q82/인코딩10FPS/새프레임대기 동일. A=8767단일origin12뷰, B=8767/8768각6뷰. 두Vite프록시 모두 상시기동. A1→B1→B2→A2,각15초준비+60초측정. 독립사용자12명 시험 아님.
- 실험용timingPlatform에절대URL인식/쿼리구분추가,시험서버에8767origin GET/CORS응답헤더노출추가. 운영 API/CORS/카메라/주행코드미변경. 사전백엔드262/프런트15테스트통과. 측정중빌드/테스트미실행.
- 고유FPS A1=8.89,B1=9.47,B2=9.66,A2=9.37. 같은요청의헤더−handler p95 A147.7/140.9ms,B55.8/53.0ms로차이가반복됨. 연결공유/프록시경로영향을시사하지만CORS/프록시분산동시변경으로유일원인확정불가.
- 나이상한p95 A1만500~750ms,나머지200~300ms. stale/missing2.17/.88/.70/.70%. A2도개선되어최신성효과확정/운영반영보류. 상한에는프레임생성전요청대기가포함되며정확한촬영종단지연이아님.
- FastAPI CPU평균51.71/52.62/52.93/53.13%(1코어100%). 전체6코어평균27.11/26.73/26.64/27.64%. CPU절감없음. B전송량증가(더많은고유프레임전달). maxCPU온도56.63℃,RSS평균162.4→170.5MiB,장기누수검증아님.
- 4단계240자원표본/24,600성공프레임타이밍/4JPEG해시검증. 숨김/overflow/영상요청/timeout/메타오류/센서중단0. A2진단보고오류1,보고커버리지90.79~91.48%. USB검색결과없음. 조명어두움,정밀화질평가보류.
- 로컬 `outputs/RGB_영상전송_실험자료/2026-09-15/02_12화면_연결분리_반복비교/` 에설계/결과/CSV/이미지/원시/재현코드. 압축602KiB,로컬원격SHA256 E7CCA623870D0063C98F72B3546C9CBB1586A153EA5E7CDF46E4C3E9E35FF539. 소스SHA E3A1968DE05954540BD1ABC7CC1594094E1091BF70CBF8427A8574D3DE7DAE67.
- GoogleSheet `0915 연결분리 반복`(2026091502),115셀정확대조. Drive https://drive.google.com/drive/folders/1gb8Zt0YttoJsImmRlN_jpBkO6Vu7fBEm 에원본JPEG4장/설계/결과/CSV,개별메타데이터이름/크기/부모확인. 공개권한변경없음. Google브라우저로그아웃으로시각검증불가,API서식확인.
- 서버2585367정상shutdown/8001해제,프록시9152/7120종료,시험탭/SSH종료. 카메라782816/782875만기존대로유지. commit/push없음.
- 다음후보: 최신성계측분리(생성전대기와생성후나이),동일JPEG단일지속연결/최신값전달/느린수신자검증. 아직미구현. 이후H.264후보비교.

## 2026-09-15 — JPEG 지속 연결 구현·ABBA·장애 시험 완료

- 실행 ID `1789436953246`, 10:49~11:03 KST. 중단 없이 11개 조건 완료: HTTP/WS 1뷰 ABBA → 12뷰 ABBA → 느린 수신자 → 강제 연결 종료 → 정상 복구. 각 15초 준비+60초 자원 측정, 브라우저 55초. RGB 640×480/Q82/인코딩 목표10FPS, 입력 약20FPS, MAXN_SUPER·6코어 동일.
- 실험 전용 WebSocket 경로(`/ws/bench/rgb`)와 바이너리 JPEG 패킷, 세션/sequence 커서 검증 구현. 브라우저 표시 완료 후 다음 최신 프레임을 요청하는 1개 대기 구조. 이미지 변환 결과 공유, 클라이언트별 누적 프레임 큐 없음. WS 메시지 압축 비활성, receive queue1, send timeout2초. 운영 API/프런트 src는 이번 변경 대상 아님.
- 최신성 독립 계측 보강: 서버가 나이를 계산하기 전 경과 시간을 전달해 요청 대기 중 아직 생성되지 않은 프레임에 대한 대기를 상한에서 제외. 100ms 관측값으로 하한/상한/기존 상한을 함께 보존. 절대 시계 동기화에 의존하지 않음. 실제 촬영 지연이 아닌 서버 RGB 콜백 수신 이후 나이. 타이밍 배열은 단계 종료 후 전송하므로 측정 중 표본별 네트워크 전송 없음.
- 사전 backend264/frontend18 테스트 통과. 12연결 프로토콜 스모크에서 JPEG 원본 바이트, sequence, 느린 소비자 최신값 건너뛰기(25프레임), 미래 커서 timeout, 잘못된 커서1008, 재접속 확인. 측정 중 빌드/테스트/분석 미실행.
- 정상 12뷰 반복 평균: FastAPI CPU 52.429→32.433%(1코어100%, 상대38.14% 감소), 고유 표시FPS 8.938→9.822. 전체 6코어 평균 CPU27.050→22.275%(4.775%p 감소). FastAPI p95 HTTP58.7/59.4%, WS37.8/37.3%. 서로 다른 실행의 p95를 평균내 전체 p95라고 하지 않음.
- 보강된 프레임 나이 상한p95: HTTP12뷰829.93/414.11ms, WS192.18/164.08ms. 1뷰 CPU 평균23.685→21.441%(상대9.48% 감소). 같은 JPEG 설정이며 더 많은 고유 프레임을 전달해 payload 전송량은 증가했다.
- 느린 수신자: 마지막 화면만 500ms 간격으로1.98FPS. 정상11개는9.60~9.85FPS, 상한p95 126~216ms 유지. >1초 표본1/6600, 진단보고오류1. 강제 연결 종료: 26.723초에12소켓 종료, 모든화면 첫표시35.6~319.1ms. 의도적 영상요청오류12, 표시정보없음18표본. 물리적 망 단절/서버 재부팅 검증은 아님.
- 검증: 11조건660자원표본, JPEG11장·메타·크기·해시, 타이밍과 frame_events 수·유한값·순서·나이 경계 확인. 숨김/overflow/미관측/새프레임timeout/메타오류/중복0, USB 오류 검색 없음. 브라우저 최신성 커버리지 정상100%, 재접속99.727%. 기존5초 진단보고 커버리지 약90~92%와 분모가 다름.
- 최대CPU온도57.09℃, RSS약157→170.5MiB 후169.8MiB 부근. 장기 메모리 누수 검증으로 해석하지 않음. 어두운 정지 차트이므로 정밀 화질/동적 장면 판단 보류. 한 브라우저12뷰/로컬Vite HTTP1.1 경로 결과이며 독립사용자12명 결과가 아님. WS fetch 헤더 시각에는 전체메시지 수신이 포함되어 HTTP 헤더 시각과 직접 비교하지 않음.
- 로컬 자료: `outputs/RGB_영상전송_실험자료/2026-09-15/03_JPEG_WebSocket_ABBA/`에 설계, 보고서, CSV2종, 상세JSON, 원본JPEG11장, 원시로그, 재현 코드, Drive manifest. 원시압축 약2.38MiB, 원격/로컬 SHA256 `F09F3E0842DD30DB9604CD4BB47DA817D1BA23EFDE97BFFE401BF49CB8C5E327`. 소스압축 SHA256 `6F37D33293F92E15C12AD966A87C5F058E39181C857F88DB59F6E178EB4BA492`.
- 기존 Google Sheets에 `0915 JPEG 지속연결`(2026091503) 추가: 288셀+설명10개 정확 대조, 서식 API 확인(네이티브 시각검증 미실시). 이미지11개 Drive 이름·크기·부모 검증. 보고서/CSV/설계 업로드. https://drive.google.com/drive/folders/1PjigDMZN1jnStshqmaAmt8iBptTQxrIm . 기존 탭/공유 권한 보존.
- 시험 서버2642478 정상 SIGINT shutdown/8001 해제, 로컬 Vite22908/시험탭/SSH 종료. 카메라782816/782875 유지. ROS_DOMAIN_ID162·LOCALHOST1, 주행/모터/YOLO/디스펜서/열화상/운영백엔드 실행 없음. git diff --check 통과. 운영 배포/commit/push 없음.
- 현재 결론: JPEG 인코딩 자체를 바꾸지 않는 지속 연결 후보가 이번 조건에서 유리함. 인증·접속 제한·운영 재시도 정책 통합 전이며 운영 준비 완료를 뜻하지 않음. H.264/WebRTC 비교는 아직 수행하지 않았음.

## 2026-09-15 — H.264/VP8 코덱 비교와 WebRTC 실물 측정

- 사용자 요청: H.264 등 선정 코덱 비교까지 수행. 실험 전용 core/screen/live Python과 frontend/tests/rgb-codec-live.html 추가. 기존 perf/rgbd-streaming-pipeline 유지, 운영 src 추가 변경 없음. ROS_DOMAIN_ID162/LOCALHOST1, 정지 RGB만 구독, 주행·YOLO·디스펜서·열화상·운영백엔드 미실행.
- Jetson FFmpeg libx264/libvpx 확인, nvv4l2h264enc 없음. 임시 /tmp/hg-rgb-codec-20260915/python에 aiortc1.14.0+PyAV16.1.0 target설치(기존venv변경없음). 기존TensorFlow 관련pip경고는보존,시험imports/실제재생통과.
- 동일 원본60장×7설정=420프레임 압축/복원/PSNR/SSIM. 초기검은프레임이PSNR을부풀린예비screen/제외.3초워밍업+평균밝기5미만거부후screen_warm/재측정,원본밝기평균40.28~40.33. 약7.14초 채집,10FPS timestamp로압축비교. 원본은npz로한번보존,ROSbag없음.
- codec settings: JPEGQ82; H264 ultrafast/veryfast CRF23,veryfast CRF28,zerolatency,baseline,B0/lookahead0/GOP10; VP8realtimecpu-used6,lag0/GOP10,500/1000/2000kbps. 모든AV인코더2스레드. 원본640×480유지. 품질계산/디코딩은압축시간밖에서수행.
- 최종동일원본: JPEG201.96KiB/s·압축CPU2.28ms/프레임·PSNR47.28dB·SSIM.9980. H264ultrafast23 79.23KiB/s·CPU5.76ms·38.42dB·.9892(데이터60.77%감소,동일화질아님). VP8 1000 122.47KiB/s·CPU13.40ms·38.63dB·.9896. H264veryfast23은이번장면에서ultrafast보다CPU/바이트높음. PNG원본+7복원본저장.
- 라이브구조: JPEG공유인코딩→WS/canvas, H264/VP8공유사전인코딩→aiortc RTP/SRTP→WebRTC video. 수신자큐2개초과시키프레임대기,PLI는공유키프레임요청. REMB가공유encoderbitrate를변경하지않는고정설정시험. 자동적응완료아님. H264선정은ultrafastCRF23의CPU/화질절충,VP8 1000은유사SSIM대안.
- 브라우저codec preference를setRemoteDescription전에양쪽명시. 초기H264바이트를VP8로협상한시험오류발견/보완/미채택. 최종getStats MIMEvideo/H264·video/VP8대조. 하단12px에프레임ID(A5+24bitseq)삽입,실제표시번호를서버수신시각과결합. 화면을움직이는모션시험을대체하지않음.
- 최신성: 전후7회clock왕복중최소RTT의offset구간을얻고두구간외곽범위사용. 구간폭약16~19ms. 서버RGB수신이후표시나이추정으로실제촬영지연/시계드리프트보장아님. 이전JPEG요청시간상한과직접합산안함.
- 정상12조건: 각코덱1→4→12→1뷰.10초준비+40초자원,브라우저35초. 말미상세로그업로드영향발견하여전조건CPU첫34표본만통일집계. 재접속3조건포함채택15조건×34=510자원표본. 원시40표본보존. 코덱별순차그룹이지12뷰ABBA/통계적유의성시험아님.
- 12뷰결과: JPEG CPU31.67%(1코어100%)/전체6코어26.16%/FPS9.83/나이상한p95 221ms. H26444.46%/19.51%/9.77/227ms. VP867.62%/25.72%/9.69/443ms. FastAPI와전체CPU방향이다름: 네트워크등경로영향가능,단일원인확정안함.
- VP8마지막1뷰: FPS9.43인데나이상한p95 921.80ms,500ms초과30%,jitterbuffer평균325.92ms,RTP손실증가0. FPS유지=최신성보장아님을관측. 모든VP8구현에일반화금지.
- 단일화면재연결: 12뷰중11번을17초에닫고새연결. JPEG54ms,H2641649.3ms,VP81436.7ms후첫표시. H264/VP8은ICE/DTLS/협상/키프레임포함. 다른11뷰최소FPS각8.89/9.69/9.80. JPEG늦은닫힌콜백guard추가후재측정(1789440722682채택,1789440350539예비제외). 화면정보없음표본1/16/15개를나이초과비율과분리. 재접속카운터초기화로payload차감치미집계.
- 비교한계발견: JPEG프레임평균바이트가초기21.5KB→12뷰33.5KB로변함. 장면/노출변화가능성배제못함. 실시간전송량비율을순수코덱효과라고하지않고동일원본60장수치로압축률판단. 차트가어둡고흐림. 제한망/패킷손실주입·동적장면·독립사용자12명·장기누수·H265/AV1/VP9미측정.
- 검증: 최종15조건센서중단/자원가드/worker에러/브라우저hidden/marker오류0. 표시ID전부history존재/시간순서/clockbound대조,최종코덱MIME대조. HTML module구문확인,gitdiff--check통과. 전체7설정디코드420프레임성공. 실제시험실행과이검증을새코드전체의운영회귀테스트완료로표현하지않음.
- 기록: outputs/RGB_영상전송_실험자료/2026-09-15/04_코덱비교/에설계/보고서/CSV3종/분석JSON/원본복원PNG8장/재현코드/원시/manifest. 아카이브약14MB,SHA256 9C0FC876E9FCEDE49FC9EE4A3541DC89E8B84B5689339F80C76E4BE4BD5C1E47(원격로컬일치).
- GoogleSheets 0915코덱실시간(2026091504)/0915코덱화질(2026091505),464셀·설명18개정확대조. Drive https://drive.google.com/drive/folders/1cFXslIVxxRyQ_xUHnHXsYnVyGBex8cap 에보고서/CSV/설계/PNG8장,메타이름크기부모검증. Google브라우저로그아웃으로native시각검증불가,API서식확인. 기존기록·공유권한보존.
- 정리: 마지막서버2820111 SIGINT정상shutdown/8001해제. 시험Vite1863세션/탭/SSH종료. 기존카메라782816/782875유지. 운영배포/commit/push없음.
- 결정권고: 기본JPEG10FPS+WebSocket유지. H264는대역폭절약이필요할때채택검토(화질/CPU/복구대기절충). VP8현재설정후순위. 최종채택전고정조명·노출반복,동적장면,제한대역폭적응검증이남음.

## 2026-09-15 — 결정된 JPEG/WS 방식을 Console 운영 코드에 통합

- 사용자 승인: 결정한 방식으로 작업하고 결과 보고. 기존 `perf/rgbd-streaming-pipeline` 유지. 이전 미커밋 실험 변경 보존, Robot 저장소 수정 없음.
- `app/rgb_stream.py`: latest-only 원본 슬롯/전용 worker, 기본 최대 10FPS, 주기 설정 검증, 종료/재설정. `RosMediaAdapter.on_rgb_image`는 수신 메타데이터와 원본만 전달. 실제 변환은 `_encode_rgb_image`로 분리.
- WS `/ws/media/rgb`: 동일 JPEG 캐시 공유, 화면별 pull-after-display, 세션/순번(계측 off에서도 유지), 204/503 구분, 서버 주기 제한, Origin 검사, 16개 연결 제한, 요청/송신 timeout, 종료 카운터 정리.
- HTTP JPEG fallback 유지. 입력 나이 2초 기준을 HTTP/WS/media status에 통일. `GET /api/v1/media/rgb/pipeline`에 누적 입력/교체/작업/실패/대기 슬롯/연결 수 제공.
- `rgbTransport.js`와 `rgbPreview.js`: WS 우선, 연결 실패 시 30초 HTTP 복구 후 재시도, 한 번에 한 요청, 요청 취소/Blob/타이머/소켓 해제, 독립적인 오래된 화면 숨김 감시, 숨긴 문서 요청 중지. Overview/Video 모두 RGB 목표 표시 간격 100ms. 열화상 간격/분석/제어 변경 없음.
- 회귀 보호: 과거 `rgb_pipeline_benchmark.py` 기준선이 새로운 worker 때문에 달라지지 않도록 기존 동기 인코딩 호출을 명시. 과거 측정물 보존.
- 검증: 백엔드 전체 272개, 프런트 전체 116개 통과, production build 성공(기존 큰 chunk 경고). 실제 route 캐시/센서 stale, 슬롯 교체, 주기/오류 복구/종료, cursor/origin/접속 한도/timeout, fallback/취소/중복/계측 off 검증.
- 로컬 브라우저: 실제 LiveImage와 새 worker/hub를 사용하는 RGB 색상 fixture. 정지 영상 숨김→재입력 자동 표시, 12개 이미지 표시, 제거 후 clients=0 확인. 10.0199초 입력256/공유 표시작업92/대기1/오류0. 당시12개 화면별5초집계44~46고유프레임, 요청/메타오류/미수신시간0. 실물 성능 측정이 아니며 Jetson 성과 수치로 사용하지 않음.
- 운영 설정·롤백·제약은 README, 상세 결과는 `docs/RGB_STREAMING_INTEGRATION.md`에 기록. 단일 worker 전제, origin 제한≠사용자 인증. 자동 적응/H264 배포/네트워크 제한/동적장면 검증은 미포함.
- 이번 턴은 Jetson 접속·운영 배포·로봇/센서 구동 변경 없음. commit/push/PR 없음. 로컬 기능 검증 서버/Vite/임시 탭 종료 후 완료. 다음은 통합 코드의 격리 실물 확인과 dev 충돌 확인.

## 2026-09-15 — 운영 통합 RGB 경로의 Jetson 실물 검증

- 사용자 요청에 따라 운영 RGB worker/hub/preview를 `/tmp/hg-rgb-integrated-20260915`의 RGB 전용 래퍼로 시험. 기존 Jetson 저장소·환경 파일·운영 서버 변경 없음. RGB 원본 토픽의 현재 수신만 사용, domain162/LOCALHOST1, 모터/임무/YOLO/디스펜서/열화상 미실행. 원래 카메라 프로세스 유지.
- MAXN_SUPER mode2, 640×480/Q82/목표10FPS, 입력 약19~20FPS. 밝은 정지 인쇄물 확인. 과거 어두운 코덱 시험과 수치를 직접 결합하지 않음. WS1→WS12→HTTP12→WS12→WS1, 각15초 워밍업+60초 측정. 독립 사용자12명이 아닌 한 브라우저의12개 이미지/연결.
- FastAPI CPU 평균(1코어=100%): WS1 21.718/21.913%, WS12 33.895/34.247%, HTTP12 56.647%. WS12 상대약39.9% 낮게 관측되지만 HTTP1회이므로 통계적 확정 개선율 아님. p95는 실행별 보존, 평균내지 않음. 전체6코어 CPU 평균: WS1 13.278/13.609%, WS12 27.611/27.855%, HTTP12 34.577%.
- JPEG인코딩: WS12 9.913회/초, WS1 9.934~9.968회/초. 화면 수와 무관하게 공유. 고유 표시FPS WS12 9.618/9.580, WS1 9.618/9.290, HTTP12 8.634. 모든 worker 실패0.
- 표시 나이 상한의 합산히스토그램p95 버킷: WS12 두번500ms, WS1 300/500ms, HTTP12 750ms. 실제 촬영지연아님. 모든표본500ms이내도아님(WS12최대700/614ms). 기존 실험의 정밀 타이밍p95와 다른 계측정의.
- 정상측정 영상요청/메타오류/미수신시간0. **WS12 1차 진단보고 전송 실패1건**: 완전포함된5초보고129개(일반132개), 영상오류아님. 상태코드/예외 상세 미기록으로 원인확정불가. 나머지창에서재전송정상. telemetry 누락을영상무손실증거로취급하지않음.
- 입력4초차단(어댑터만)→오래된화면숨김true→복구표시true. 화면제거후연결0→재생성후표시true. 물리USB/망단절/서버재시작시험아님. fault-check.json에브라우저결과보존.
- 5조건×60=300자원표본, 전체구간포함보고만사용, 보고중복/화면수/숨김·미관측/나이hist합계검증통과. CPU는Python집계와원시JSON SQLite집계교차검증. RSS최대약158~166MiB,Swap0,CPU온도최대57.062℃. 장기메모리누수검증아님.
- 자료: `outputs/RGB_영상전송_실험자료/2026-09-15/05_운영통합검증/` 원시/프레임5장/CSV/analysis.json/검증결과.md/재현스크립트. SHA256 `47B1D7D0A159DA2EB08F49CE84C4FF9023BB2A08B40F597A221D2575327025BA` 원격/로컬일치. canonical HTML 패키저 가로넘침 QA실패로 HTML미배포, Markdown/CSV정본제공.
- 테스트 PID2951924 SIGINT 정상종료,8001해제,tegrastats없음,로컬Vite8767/시험탭/SSH종료. 카메라782875유지. 실제 전체backend(main)/SLAM동시부하/운영배포/commit/push미수행.

## 2026-09-15 — GPU 전처리 5회 간이 선별

- 요청: GPU가 이득인지 약5회 확인. Jetson MAXN_SUPER2 그대로, 읽기전용 RGB 입력만 사용. 운영변경·주행·센서재시작없음.
- 사용자 OpenCV4.10.0은 CUDA0/함수없음, 시스템 `/usr/local/lib/python3.10/dist-packages/cv2`는 CUDA1/cvtColor지원. 시스템 OpenCV를 `PYTHONNOUSERSITE=1`로 시험 명령에만 선택, 재설치 없음. nvcc존재, CUDA toolkit nvJPEG라이브러리/헤더는 검사경로에없음.
- 실시간 RGB15초수신0, domain162에는parameter_events/rosout만발견. 기존카메라PID782875는존재. 이전실물screen_warm의원본30프레임으로대체;실시간측정이라고표현하지않음.
- 3경로×5회×30프레임=450,10FPS간격각3초,순서회전·워밍업제외. BGR직접CPUJPEG / 사전RGBfixture→CPU색변환+CPUJPEG / 동일RGBfixture→CUDA색변환+CPUJPEG. GPU는upload/wait/download포함. 모든경로동일CUDA지원OpenCV·threads6·640480·Q82. CUDA반환객체처리오류는사전정확성검증에서수정후측정.
- 평균처리시간4.71/5.00/6.38ms, 전처리없음/0.24/1.62ms, 시험프로세스CPU4.74/11.78/5.76%(1코어100%). 조건부RGB변환에서는GPU가CPU시간감소하지만walltime증가. 실제BGR경로는변환이불필요하므로현재경로유지. 전체FastAPI CPU와비교금지. 서로다른cv2빌드의운영백엔드직접전후비교도아님.
- 30원본에대해각경로픽셀/JPEG바이트동일,450측정처리100ms초과0,각경로5회완료. 짧은선별시험이며 GPU압축/전송가속/장기통합안정성결론아님.
- `outputs/RGB_영상전송_실험자료/2026-09-15/06_GPU전처리_5회/`에README·원시·반복CSV·환경·입출력이미지·스크립트저장. archiveSHA256 `972F9417ABC450EC42BF049DBB8B7EAC60A00DE3C6AC3C37BE600583D63D55E9` 원격/로컬일치. 시험프로세스/tegrastats/SSH종료,기존센서프로세스유지. commit/push없음.

## 2026-09-17 — 최신성 우선 가변 JPEG/H.264 실험 경로 추가

- 사용자 요청: 네트워크/CPU 부하에 따라 압축 설정과 JPEG/H.264를 조절. 기본640×480/10FPS, 화면RGB만 변경, 원본Depth/열화상/탐지 보존. 10월까지 검증 우선.
- 기존 이슈29 / `perf/rgbd-streaming-pipeline`에서 진행. 기존 dirty 변경은 보존. 이전 JPEG 결정은 당시 조건의 운영 선택으로 한정하며, H.264 바이트 절감과 CPU/복구 비용을 같은 전송 경로로 재비교하도록 계획 수정.
- 신규 `rgb_adaptive.py`: latest 원본/압축슬롯 각1, 공유 인코더1, JPEG Q82/70/55, H.264800/400kbit/s, FPS10/8/5. 지속 ACK 지연과 CPU 여유에 따라8초 H.264 시험, 최신성/압축이득/CPU 보호에 따른 JPEG 복귀,60초 cooldown, 느린 상향 복구. 현재 임계값은 검증 가설임.
- `rgb_adaptive_transport.py`: 한 프레임/ACK, sequence/epoch 검증, H.264 참조 누락 시 키프레임 요청, SPS에서 WebCodecs 코덱 문자열 추출. 새 코덱 평가에 이전 epoch ACK가 섞이지 않도록 보완하고 테스트 추가.
- `rgbAdaptivePreview.js` + `rgb-adaptive-harness.html`: ImageBitmap/WebCodecs Canvas 표시, 표시 직전 나이 검사, stale 숨김, timeout/재연결, 미지원 JPEG fallback, 자원 해제. 운영 Overview/Video는 이 실험을 import하지 않음.
- `rgb_adaptive_experiment.py`: 별도 FastAPI, synthetic/NPY/읽기전용 ROS RGB 입력, 단일 worker, 기본loopback8002. 원본센서/주행프로그램 기동 없음. psutil CPU/RSS, 바이트/전환/오류 JSONL1Hz,4MB×3파일 회전. 계측 CPU는 전체 평균과 프로세스1코어100%를 구별.
- 별도 `.venv-adaptive`에 실험 의존성 설치. 운영 의존성 변경 없음. 새 `requirements-adaptive.txt`, 재현/비교 조건/지표 한계/출처를 `RGB_ADAPTIVE_EXPERIMENT.md`에 기록하고 README에서 연결.
- 최종 검증: 백엔드 전체290개, 프런트 전체118개(마지막 lifecycle 보완 후 신규2개 재통과), Vite build 성공. 기존 큰chunk경고와 FastAPI/Starlette deprecation경고 있음. 실제 PyAV 인코딩·decode·누락후키프레임복구 및 같은WS의 JPEG→H.264→JPEG 전환 통과. 자동전환 정책 입력은 결정론적 fixture이며 실제망부하 시험이 아님.
- computer-use로 실제 내장브라우저 확인: H.264표시869프레임 시점 오류/폐기/재연결0의 JSON 저장. 서버중단시 빈화면 확인, auto서버재시작후 JPEG로 자동복구 확인. 마지막 독립 JPEG시작에서는 표시502/오류0/폐기0 확인. 로컬 합성사각형 시험이며 Jetson 성능/화질 결과로 사용하지 않음.
- 자료: `outputs/rgb-adaptive/2026-09-17/local-h264/`(samples.jsonl, browser-observation.json), `local-auto/`(samples.jsonl). 브라우저 JSON은 최근600표본이므로 전체실험 p95와 구별. PNG/추가 JSON 다운로드는 파일 존재를 확인하지 못해 산출물로 집계하지 않음.
- 종료 전 clients0/pending0/encode실패0/protocol오류0 확인. 이번에 띄운8002/8772 서버 종료. Jetson 접속/배포/운영변경/commit/push는 하지 않음.
- 남은 검증: Jetson 실제 인코더·브라우저 지원, 고정JPEG/H.264/auto 조건교차3회, 망제한/CPU부하/접속수/복구/동적장면, 전체구간 클라이언트 지표 수집기 및 품질 대응 프레임 기록 보강. 실측 후 임계값 확정 및 운영 UI 연결. 현재 프로토타입을 운영완료로 간주하지 않음.

### 후속 빠른 검증 — SSH 인증 차단

- 사용자 요청에 따라 Jetson 실측을 시도. Tailscale에서 yahboom 온라인 및 SSH 서버 응답 확인, 공개키 인증과 기존에 제공된 비밀번호 인증은 거부됨. 반복 비밀번호 추측 없이 종료하고 현재 계정 확인 요청.
- 로컬 적응형 전송 전용 회귀 테스트 재실행: 백엔드18개(1.46초), 프런트2개 통과. 실제 PyAV 및 WS JPEG→H.264→JPEG, 키프레임 복구, CPU 보호, epoch별 ACK 분리 포함. FastAPI/Starlette deprecation 경고2개.
- Jetson RGB 수신·CPU·대역폭은 이번에 측정하지 못함. 원격 코드/설정/프로세스 변경 없음. 로컬 결과를 실물 성능 결과로 대체하지 않음.

### 후속 재시도 — 인증 정정, 실물 빠른 선별 완료

- 앞선 인증 차단은 기기명(yahboom)과 로그인계정(jetson)을 혼동한 작업자 오류였다. 공식 Yahboom 자료 확인 후 기존 기본계정으로 성공. 자격증명은 로그/코드에 저장하지 않음.
- HP60C 프로세스782816/782875는 있으나 RGB 미수신, 로그에 USB제어실패/No such device. 같은overlay/domain162/LOCALHOST1로 카메라launch만 정상종료 후 재시작(PID3891503). 실제RGB헤더 수신 확인. 다른장치/주행 미실행.
- `/tmp/hg-rgb-check-20260917.OMAHwQ`에 신규실험코드만 복사. 기존 운영venv와 기존 실험 PyAV16.1.0을 PYTHONPATH로 재사용, 패키지/운영저장소 변경 없음.
- 실물에서 발견한 신규 ROS source 버그 수정: custom Context에 global executor 사용 시 AttributeError __enter__. 같은 Context의 SingleThreadedExecutor 생성·종료로 보완. 로컬 전용18개 회귀 재통과, 실제RGB 수신 후4조건 완료.
- 10초워밍업+30초측정×4, JPEG/H.264/auto/auto+ACK160ms지연. 새로운 headless quick client는 desktop에서 복원만 수행, Canvas표시 아님. 사용자 요청대로 이미지캡처/저장 제외. 운영브라우저/실제망제한/화질시험 결과와 구별.
- JPEG/H.264/auto 정상: 복원9.87/9.73/9.93FPS, 응용59.23/36.94/59.65KiB/s, 수신후나이상한p95 172.1/182.2/175.8ms. 서버 프로세스CPU평균14.63/17.41/14.79%(1코어100%). CPU는 active1초표본 첫10개제외 다음30개이며 클라이언트창과 경계동기화되지 않음.
- 압력조건은 JPEG→H.264→800/400kbit/s→목표8FPS까지 조절, 인코더실패0. 단 ACK인위지연이며 네트워크용량제한 아님. 키프레임요청최종73회, 측정131프레임중키71개로 복구비용 확인. 운영채택결론 유보.
- JPEG/H.264의500ms초과나이상한 각1건,503상태도 워밍업포함 일부발생. 무장애/모든프레임최신성통과로 표현하지 않음. Windows monotonic 타이머 약15ms양자화로 세밀한decode_ms 비교 불가.
- 자료와 상세한계: `outputs/rgb-adaptive/2026-09-17/jetson-quick/README.md`, 조건별client.json,server원시JSONL/실행코드. 시험서버/터널/SSH 종료,카메라정상프로세스유지. commit/push없음.

### 후속 — 적응형 운영 연결 구현 및 최종 동작 회귀

- 사용자 요청: 남은 구현을 완료하고 포트폴리오 문구를 실제 완료 범위에 맞춰 다시 작성.
- `AdaptiveRgbRuntime` 추가: 기존 ROS RGB callback 입력을 공유세션으로 전달, 별도 ROS 구독/제어 없음.
  backend `HAZARD_GUARD_RGB_ADAPTIVE=on` opt-in, 기본off. Overview/영상 페이지는 미디어 상태의 adaptive 필드로 Canvas 경로 선택.
  `/ws/media/rgb/adaptive`, 읽기전용 상태API,1Hz CPU/프로세스/정책 로그4MB×3개 회전 추가.
- JPEG 레거시 소비자 요청 후1초만 호환 JPEG 캐시를 생성해 기본 이중 인코딩 방지. 스냅샷/API/기존 WS 유지.
  실제 열화상 미수신 때의 기존 RGB 유래 가상 열화상 변환은 범위 밖이므로 보존. 원본 Depth/열화상/탐지/Robot 저장소 변경 없음.
- 최신 입력/결과1개와 미확인 전송1개 유지. H.264는 수신자 모두 ACK 대기 중이면 인코딩도 대기.
 10초간 키프레임 요청12회 이상이면 JPEG 복귀. 성공한 H.264 시험 이후 안정된 RTT 자체로 계속 품질/FPS를 낮추지 않도록 수정.
- malformed hello/종료 중 클라이언트 제거/미지원 크기/설정off/센서 stale/재시작/생산 API·호환JPEG 회귀 추가.
  Jetson의 구버전 Starlette가 close에서도 WebSocketDisconnect를 내는 경우 정상 종료 처리하도록 보완하고 자동 테스트 추가.
- 브라우저 실측에서 숨김 창의 rAF 지연을 발견. 디코딩 오류와 paint 지연을 구분하고, paint timeout에서는 연결/디코더 해제 후
  wake callback1개만 유지하도록 수정. 오래된 미표시 프레임/예약 rAF 해제, JPEG/H.264 전환·늦은bitmap·화면 갱신 재개 테스트 추가.
- Windows 계측시계 perf_counter로 변경, 측정창 끝 프레임 제외. 앞선 monotonic 결과와 엄밀한 전후 개선율 비교 금지.
- Jetson 격리실측: auto+ACK160ms(10초워밍업+30초)4.73복원FPS/17.89KiB/s/나이상한p95 126.04ms.
  JPEG→H.264 채택, 키프레임요청/encode실패/protocol오류/timeout0. 실제망제한이 아닌 피드백 지연 시험임.
- 혼합 수신: 약10초 시차 시작, 각각5초워밍업+20초, 빠른쪽7.50FPS/p95 262.32ms, 느린쪽4.85FPS/p95 206.07ms.
  참조복구과다에 따른 JPEG 복귀 확인. 일부구간만 동시수신이므로 동기화된 접속수 성능비교로 표현하지 않음.
- 최종 실제 브라우저 H.264 표시752프레임 시점 폐기/실패/재연결/paint_pause0, 최근600개 나이상한p95 182.01ms.
  화면 이미지 캡처는 사용자 요청대로 하지 않음. 이 관찰은 전체 E2E지연/FPS 보장/장기 안정성 결과가 아님.
- 최종 backend300개/frontend123개 통과, Vite build 성공. 기존 Starlette deprecation2개 및 큰번들 경고 남음.
- 자료 `outputs/rgb-adaptive/2026-09-17/implementation-check/` 분류보관. 서버원본archive SHA256
  `040cb01a2954baf2cb243bf375326588791c12207827cbbcb2b39e799fd89bb8` 원격/로컬일치.
- 운영 Jetson 저장소/백엔드는 변경하지 않음. 신규 임시서버/터널/SSH/로컬8772프런트/시험탭 종료, 기존카메라유지.
  README·실험설계·적용/rollback·포트폴리오문구 갱신. commit/push하지 않음.
- 남은 범위는 실제망 용량 제한·반복 정량비교·장시간·통합부하를 통한 정책 튜닝과 운영 배포다.
  과거60.8%는 동일60프레임10FPS 환산 코덱 비교값이며 이 적응형 구현의 실측 절감률이라고 쓰지 않는다.

### 고도화 브랜치 커밋·푸시 정리

- 사용자 요청에 따라 `perf/rgbd-streaming-pipeline`의 누적 작업을 백엔드 런타임/프런트 통합/실험 도구/문서의4개 커밋으로 구분.
- 이슈29를 각 커밋 본문에서 참조. dev/main 병합 및 운영 Jetson 배포는 하지 않는다.
- 새 적응형 런타임 로그도 gitignore에 추가. 실험 원본/이미지/가상환경/환경변수 파일과 자격증명은 커밋하지 않는다.
- 선택 의존성 설명을 실험 전용에서 opt-in 운영 및 실험용으로 정정. 기능 변경 없이 기존 최종 검증 결과300/123개 통과를 유지.
